import { styled } from '@linaria/react'

export const GitLabSettings = styled.div`
  display: flex;
  flex-direction: column;
  flex-grow: 1;
  padding: 16px;
  gap: 12px;
`

export const Field = styled.label`
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 4px;
`

export const CheckButton = styled.button`
  align-self: center;
`

export const Title = styled.h2`
  margin: 0;
  font-size: inherit;
  font-weight: 600;
  line-height: inherit;
`
